# student-result-management
Student Result Management System is a Python-based application that manages student records using SQLite3. It supports adding, viewing, updating, deleting, and searching student data. The system uses Pandas and NumPy for performance analysis, topper identification, class averages, and report generation.
